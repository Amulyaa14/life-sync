{
  "name": "life-sync",
  "version": "1.0.0",
  "description": "Life Sync is a full-stack personal productivity web application built using the MERN stack (MongoDB, Express, React, Node.js). \r It features a daily task manager, health tracker, and an AI-assisted natural-language routine planner that automatically generates schedules.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}